import { unsafeHTML } from "lit-html/directives/unsafe-html";
import { langChanged } from "../directive";
import { get } from "../util";
/**
 * A lit directive that updates the translation and renders embedded HTML markup when the language changes.
 * @param key
 * @param values
 * @param config
 */
export const translateUnsafeHTML = (key, values, config) => langChanged(() => unsafeHTML(get(key, values, config)));
//# sourceMappingURL=translate-unsafe-html.js.map