import { ITranslateConfig, Values, ValuesCallback } from "../model";
/**
 * A lit directive that updates the translation when the language changes.
 * @param key
 * @param values
 * @param config
 */
export declare const translate: (key: string, values?: Values | ValuesCallback | undefined, config?: ITranslateConfig | undefined) => (part: import("lit-html").NodePart) => void;
