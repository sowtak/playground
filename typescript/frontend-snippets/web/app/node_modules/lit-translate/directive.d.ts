import { NodePart } from "lit-html";
import { LangChangedDirectiveCallback } from "./model";
export declare const partCache: Map<NodePart, LangChangedDirectiveCallback>;
/**
 * A lit directive that invokes the callback when the language changes.
 * @param key
 * @param values
 * @param listen
 */
export declare const langChanged: (cb: LangChangedDirectiveCallback) => (part: NodePart) => void;
